%h1 Add Multiple Choice Answer
.row
  .col-md-6.col-md-offset-3
    = form_for(@choice) do |f|
      = f.label :choice
      = f.text_field :choice, class: "form-control"

      = f.submit "Submit",  class: "btn btn-primary"
